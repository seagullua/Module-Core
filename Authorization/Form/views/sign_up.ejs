<% pageTitle(__('auth.sign_up.form_title')) %>

<div class="container big-block">
    <div class="col-md-8 col-md-offset-2">
        <h1><%- __('auth.sign_up.form_title') %></h1>

        <div class="registration-form">
            <div class="step">
                <form method="post">
                    <p class="text-justify"><%- __('auth.sign_up.form_description') %></p>

                    <div class="form-group">
                        <label for="email-big"><%= __('auth.sign_up.email_label') %></label>
                        <input type="text" id="email-big"
                               class="form-control input-large"
                               placeholder="<%= __('auth.sign_up.email_placeholder') %>"
                               name="email"
                               value="<%= email %>"
                                />

                    </div>

                    <div class="form-group">
                        <label for="password-big"><%= __('auth.sign_up.password_label') %></label>
                        <input type="password" id="password-big"
                               class="form-control input-large"
                               placeholder="<%= __('auth.sign_up.password_placeholder') %>"
                               name="password"
                               value="<%= password %>"
                                />
                    </div>

                    <% if(message) { %>
                    <div class="alert alert-danger"><%- __(message) %></div>
                    <% } %>

                    <input type="submit" id="sign-up-button"
                           class="btn btn-lg btn-default pull-right next"
                           value="<%- __('auth.sign_up.button_sign_up') %>"/>
                </form>
            </div>
        </div>
    </div>
</div>
